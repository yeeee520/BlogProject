# 相册"公开/私密分级"需求文档 v2

> **给 AI Agent 的指令**：请先通读本文件，然后读取项目中的相关源码文件（init.sql、springboot/src 下的 Entity/Mapper/Controller/Service、vue/src 下的 views/api/stores/components），理解现有代码结构后再按本文档要求逐步实现。实现过程中遇到本文档未覆盖的模糊决策，使用 Plan 模式列出让用户确认。

> 本文档是本次需求的唯一真理源，所有实现以此为准。

## 一、项目背景

### 技术栈
- 后端：Spring Boot（Java），已实现 JWT 登录（JwtUtil 工具类）
- 前端：Vue 3（组合式 API），Vue Router，Element Plus
- 数据库：MySQL，所有表结构定义在 `init.sql`（唯一真理源，变更必须先改它再提交 Git）
- 文件存储：腾讯云 COS
- 部署：腾讯云 ECS

### 环境配置
- 后端 systemd 服务名 `blog-backend`，端口 8080
- 前端 Nginx 端口 80，静态文件目录 `/var/www/blog/`
- 本地开发数据库 `code2026`，生产数据库 `blog`
- 本地启动：`mvn spring-boot:run -Dspring-boot.run.profiles=local`

### 管理员账号
- 用户名：`yeeee`
- 密码：通过安全渠道保存，数据库仅存 BCrypt 哈希
- 登录接口返回 JWT Token，前端存于 `localStorage`

### 交流约定（请严格遵守）
- 始终用中文回复
- 每次主要操作前称呼用户"老哥"
- 遇到模糊决策时，不要直接做选择，而是把问题和选项用 **Plan 模式**列出来让用户确认
- 本需求文档是唯一上下文来源，不需要再次解释项目背景，直接读取项目文件后开始实现
- 实现前先通读相关文件（init.sql、后端 Entity/Mapper/Controller/Service、前端 views/api/stores），确认理解现有代码结构后再动手
- 本需求文档同时作为完整 Prompt，新开对话时直接粘贴即可，不需要额外解释项目背景

---

## 二、需求目标

相册功能从"全隐藏/全显示"升级为**公开/私密混合模式**：

- 导航菜单中的"相册"入口对所有人（包括未登录访客）可见。
- 部分照片（如风景、技术配图）可设为**公开**，所有人可见。
- 部分照片（如私人生活照）保持**私密**，仅管理员可见。

---

## 三、核心设计决策

### 3.1 可见性控制粒度：照片级（方案 A）

在 `album_photo` 表上增加 `is_public` 字段，按单张照片控制可见性。
当前没有独立的 album 表，"相册"仅是 `album_name` 分组。暂不新建 album 表。

**必须支持批量操作**：管理员可在相册管理页面批量切换多张照片的公开/私密状态。

### 3.2 `status` 与 `is_public` 的优先级

| status | is_public | 实际行为 |
|--------|-----------|----------|
| 1 | 1 | 公开，所有人可见 |
| 1 | 0 | 私密，仅管理员可见 |
| 0 | 1 | **不展示**（status 优先级更高） |
| 0 | 0 | **不展示** |

规则：**`status=0` 的照片无论 `is_public` 如何都不展示给任何人**。`is_public` 仅在 `status=1` 的前提下生效。

### 3.3 可见性权限模型

| 角色 | 能看到什么 |
|------|-----------|
| 未登录访客 | 仅 `status=1 AND is_public=1` 的照片 |
| 管理员（有效 JWT） | 所有 `status=1` 的照片（无论 is_public） |

说明：当前系统仅有一个管理员账号，不开放游客注册/登录。未来若开通普通用户登录，普通用户登录后也**只能看到公开照片**，私密照片**不论何时都仅管理员可见**。

### 3.4 前端默认状态：未登录

当前系统默认打开网页就是登录状态，需改为**默认未登录**。用户主动登录后才显示管理入口和私密内容。

### 3.5 安全加固

#### SecurityConfig 权限区分
```
GET  /api/album/**  → permitAll()    （公开读取，后端再按 is_public 过滤）
POST /api/album/**  → authenticated() （写操作必须登录）
PUT  /api/album/**  → authenticated()
DELETE /api/album/** → authenticated()
```
写操作（上传/编辑/删除照片）在 SecurityConfig 层面强制要求有效 JWT。

#### Token 过期处理
- 后端返回 401 时，前端**静默降级**：
  - 自动清除 `localStorage` 中的过期 token
  - 更新 authStore 登录状态为未登录
  - **不跳转登录页**（公开内容仍可浏览）
  - 如果用户正在管理页面（如 AlbumAdmin），跳转到登录页

#### 数据库索引
为 `is_public` 字段增加索引，优化公开照片查询性能：
```sql
KEY idx_is_public (is_public)
KEY idx_visibility (status, is_public)  -- 组合索引
```

---

## 四、详细实现要求

### 4.1 数据库变更（init.sql）

#### 修改 `album_photo` 表定义
增加字段：
```sql
is_public  TINYINT(1)  NOT NULL DEFAULT 0  COMMENT '是否公开：0=私密（仅管理员）, 1=公开（所有人可见）'
```
增加索引：
```sql
KEY idx_is_public (is_public),
KEY idx_visibility (status, is_public)
```

#### 迁移语句（ALTER）
现有数据默认设为 0（私密），保护已有照片：
```sql
ALTER TABLE album_photo
  ADD COLUMN is_public TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否公开：0=私密, 1=公开' AFTER status,
  ADD KEY idx_is_public (is_public),
  ADD KEY idx_visibility (status, is_public);
```

#### 管理员账号变更
将现有管理员账号修改为 `yeeee`，密码仅以 BCrypt 哈希保存。提供 UPDATE 语句。

### 4.2 后端变更

#### 实体类 `AlbumPhoto.java`
增加字段：
```java
private Integer isPublic;  // 0=私密, 1=公开
// getter / setter
```

#### Mapper `AlbumPhotoMapper.xml`
- `insert`：增加 `is_public` 字段
- `update`：增加 `is_public` 字段的条件更新
- `selectAll`：增加 `publicOnly` 参数，当 `publicOnly=true` 时追加 `AND is_public = 1`
- `selectTags`：增加 `publicOnly` 过滤
- `selectAlbumNames`：增加 `publicOnly` 过滤

#### Mapper 接口 `AlbumPhotoMapper.java`
- `selectAll` 方法增加 `@Param("publicOnly") Boolean publicOnly` 参数
- `selectTags` 方法增加 `@Param("publicOnly") Boolean publicOnly` 参数
- `selectAlbumNames` 方法增加 `@Param("publicOnly") Boolean publicOnly` 参数

#### `AlbumController.java`
- 查询接口根据 `SecurityContextHolder` 判断当前用户身份：
  - 未登录（无有效 Token / Token 过期）→ `publicOnly = true`
  - 管理员（有效 JWT）→ `publicOnly = false`
- 详情接口：未登录访问私密照片返回 404（不暴露存在）
- 上传/编辑/删除接口：SecurityConfig 已限制为 `authenticated()`，无需额外检查
- 编辑接口支持传入 `is_public` 参数

#### `AlbumService.java`
- `listPhotos` / `listPhotosByAlbum` 增加 `publicOnly` 参数
- `getPhoto` 增加可见性检查：未登录用户访问私密照片返回 null
- `uploadPhoto` 支持设置 `isPublic`，默认 0
- `updatePhoto` 支持更新 `isPublic`
- `getAllTags` / `getAllAlbumNames` 增加 `publicOnly` 参数

#### `SecurityConfig.java`
修改 album 相关权限配置：
```java
.requestMatchers(HttpMethod.GET, "/api/album/**").permitAll()
.requestMatchers("/api/album/**").authenticated()
```

### 4.3 前端变更

#### `request.js`（axios 拦截器）
增加 401 响应处理：
```javascript
// 响应拦截器中增加：
if (error.response?.status === 401) {
  localStorage.removeItem('token')
  // 更新 authStore（需要导入或通过事件通知）
}
```

#### `stores/auth.js`
- 默认状态为未登录（不自动从 localStorage 恢复 token 时设为"已登录"）
- 增加 `clearAuth` 方法供 401 时调用

#### `Album.vue`（相册列表页）
- 导航菜单保留"相册"入口，对所有访客可见（当前已是如此）
- 页面加载时请求公开照片列表，后端自动过滤，前端无需额外判断
- Token 过期时静默降级，公开内容仍可浏览

#### `AlbumAdmin.vue`（相册管理页，仅管理员可见）
- 编辑对话框增加**公开开关**（`el-switch`），文案："允许所有人查看"
- 批量上传时增加**默认公开开关**，默认关闭（is_public=0）
- 照片表格增加"可见性"列：公开显示为绿色标签，私密显示为灰色标签+小锁图标
- 支持**批量切换公开状态**：选中多张照片 → 一键公开 / 一键私密

#### `album.js`（API）
- `batchUploadPhotos` 支持传递 `is_public` 参数
- 新增 `batchUpdateVisibility(photoIds, isPublic)` 批量更新可见性接口

---

## 五、约束条件

1. 任何 SQL 变更必须先改 `init.sql`，再提交 Git
2. 不能破坏本地开发环境（profile=local）
3. 不能影响现有的 `status`（显示/隐藏）功能
4. 代码保持简洁，加必要的中文注释
5. 使用现有的 JwtUtil，不重复造轮子

---

## 六、交付内容

1. `init.sql` 更新 + ALTER 迁移语句 + 管理员账号 UPDATE 语句
2. 后端 Controller / Service / Mapper / Entity 修改代码
3. 前端相册列表页、管理页、API、请求拦截器修改代码
4. SecurityConfig 权限配置修改
5. 本地测试步骤（重点验证：退出登录后只能看到公开照片，登录后看到全部，写操作必须登录）

---

## 七、本地测试步骤

### 7.1 数据库迁移
```sql
-- 执行 init.sql 中的 ALTER 语句
-- 执行管理员账号更新语句
```

### 7.2 后端验证
```bash
mvn spring-boot:run -Dspring-boot.run.profiles=local
```
1. 不带 Token 请求 `GET /api/album/photos?status=1` → 只返回公开照片
2. 带有效 Token 请求 `GET /api/album/photos?status=1` → 返回所有可见照片
3. 不带 Token 请求 `POST /api/album/photos` → 返回 401
4. 不带 Token 请求私密照片详情 → 返回 404
5. 不带 Token 请求 `GET /api/album/tags` → 只返回公开照片的标签
6. 不带 Token 请求 `GET /api/album/albums` → 只返回包含公开照片的相册名

### 7.3 前端验证
1. 打开网页 → 默认未登录状态，导航栏显示"登录"按钮
2. 点击"相册" → 只看到公开照片，私密照片不显示
3. 登录管理员账号 → 看到所有照片，管理入口出现
4. 在管理页面 → 可以切换照片的公开/私密状态
5. 批量上传照片 → 默认为私密状态
6. 退出登录 → 私密照片消失，公开照片仍可浏览
7. Token 过期后 → 静默降级为未登录状态，不报错不跳转
