# 云服务器运维手册（安全版）

## 目录约定

```text
/opt/blog/springboot.jar                    后端程序
/etc/blog-backend.env                       生产密钥（root:root，600）
/var/www/blog/                              前端静态文件
/etc/nginx/sites-available/blog             HTTPS 反向代理
/etc/systemd/system/blog-backend.service    后端服务
```

仓库里的 `application.yaml` 不保存生产密码。本地使用被 Git 忽略的 `application-local.yaml`，数据库为 `code2026`；服务器环境变量中的数据库为 `blog`。

## 更新后端

```bash
cd ~/BlogProject
git pull
cd springboot
mvn clean package
sudo systemctl stop blog-backend
sudo install -o blogapp -g blogapp -m 750 \
  target/springboot-0.0.1-SNAPSHOT.jar /opt/blog/springboot.jar
sudo systemctl start blog-backend
sudo systemctl status blog-backend
```

## 更新前端

```bash
cd ~/BlogProject/vue
npm ci
npm run build
sudo cp -r dist/. /var/www/blog/
sudo nginx -t
sudo systemctl reload nginx
```

## 数据库迁移

`init.sql` 是新建数据库的结构真理源。已有生产库不要反复运行建库脚本，应先备份，再只执行尚未执行的迁移：

```bash
sudo mysqldump blog > ~/blog-before-migration.sql
sudo mysql blog < ~/BlogProject/album_visibility_migration.sql
sudo mysql blog < ~/BlogProject/security_hardening_migration.sql
```

迁移使用数据库管理员权限；后端运行账号只授予 `blog` 库的 `SELECT, INSERT, UPDATE, DELETE`。

## 常用检查

```bash
sudo systemctl status blog-backend
sudo journalctl -u blog-backend -n 100 --no-pager
sudo nginx -t
sudo tail -n 100 /var/log/nginx/error.log
ss -lntp
curl -i http://127.0.0.1:8080/api/album/photos
```

正常情况下，8080 和 3306 只在本机监听或被防火墙阻断；公网只开放 80、443，以及受来源 IP 限制的 22。

## 密钥轮换

- 数据库、JWT、COS 密钥只修改 `/etc/blog-backend.env`，随后 `sudo systemctl restart blog-backend`。
- 更换 `JWT_SECRET` 会使所有现有登录立即失效，这是预期行为。
- COS 使用最小权限子账号；一旦怀疑泄露，先禁用旧密钥，再生成并替换新密钥。
- 不要把 `/etc/blog-backend.env`、证书私钥、数据库备份上传到 Git 或聊天记录。

完整首次部署、腾讯云免费证书和验收步骤见 [安全部署指南.md](安全部署指南.md)。
