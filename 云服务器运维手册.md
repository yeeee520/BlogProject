# 云服务器运维手册

> **服务器信息**
> - 云服务商: 腾讯云 ECS（CVM）
> - 系统: Ubuntu 22.04 LTS
> - 配置: 4 核 4GB
> - 公网 IP: 159.75.99.61
> - SSH 用户: ubuntu
> - 域名: （购买后配置）

---

## 一、远程连接服务器

### 1.1 SSH 登录

```bash
ssh ubuntu@159.75.99.61
```

首次连接会提示确认指纹，输入 `yes` 即可。如果设了密钥对，不需要密码；如果用密码登录，输入你的 SSH 密码。

### 1.2 退出登录

```bash
exit
```

### 1.3 传文件（SCP）

```bash
# 从本地传到服务器
scp 本地文件路径 ubuntu@159.75.99.61:~/目标路径

# 从服务器下载到本地
scp ubuntu@159.75.99.61:~/远程文件路径 本地路径
```

---

## 二、项目在服务器上的布局

```
/home/ubuntu/blog/                   # 后端外部配置
  └── application.properties         # 被 systemd 加载覆盖 yml 配置

/var/www/blog/                        # 前端静态文件（Nginx 根目录）
  ├── index.html
  └── assets/

/etc/nginx/sites-available/blog       # Nginx 配置文件
/etc/nginx/sites-enabled/blog         # 软链接到 sites-available

/etc/systemd/system/blog-backend.service  # 后端 systemd 服务
```

---

## 三、数据库同步（最重要）

### 3.1 真理源原则

**`init.sql` 是数据库表结构的唯一真理源。** 所有表结构变更必须：
1. 改完 Java 实体类 -> 先更新 `init.sql`
2. 把 `init.sql` 和代码一起提交 Git
3. 服务器上跑 `init.sql` 同步

### 3.2 首次部署或重建数据库

服务器用 `blog` 数据库，本地用 `code2026`。执行前先改数据库名：

```sql
-- init.sql 前两行，用哪个就取消注释哪个：
USE blog;       -- 服务器
-- USE code2026; -- 本地开发
```

然后执行：

```bash
mysql -u root -p123456 < init.sql
```

`CREATE TABLE IF NOT EXISTS` 保证：
- 已有表不会动（数据安全）
- 缺少的表会自动创建
- 可重复执行，不会报错

### 3.3 日常同步流程

```
你修改了 Java 实体类（比如给 User 加了字段）
        |
        v
1. 更新 init.sql 中对应的 CREATE TABLE
   （把新字段加到建表语句里）
        |
        v
2. 提交 Git（代码 + init.sql 一起提交）
        |
        v
3. 服务器 git pull
        |
        v
4. 服务器执行同步：
   ssh ubuntu@159.75.99.61
   mysql -u root -p123456 blog < init.sql
        |
        v
5. 如果只是新增字段，需要 ALTER TABLE：
   ALTER TABLE user ADD COLUMN 新字段名 类型 COMMENT '说明';
   （这条命令要提前在本地测试过再上服务器）
```

### 3.4 注意事项

- 永远不要手动在服务器上 ALTER TABLE，不先在本地测试
- 如果 init.sql 和服务器表结构出现差异，先排查本地代码，确保 init.sql 是最新的
- 删除字段要谨慎 -- 确保没有代码依赖它

---

## 四、部署更新

### 4.1 完整部署流程

```bash
# 1. SSH 到服务器
ssh ubuntu@159.75.99.61

# 2. 拉取最新代码
cd ~/BlogProject
git pull

# 3. 编译后端
cd springboot
mvn clean package -DskipTests

# 4. 重启后端服务
sudo systemctl restart blog-backend
sudo systemctl status blog-backend   # 确认运行状态

# 5. 构建前端
cd ~/BlogProject/vue
npm install          # 首次或依赖有更新时才需要
npm run build

# 6. 复制前端到 Nginx 目录
sudo cp -r dist/* /var/www/blog/
```

### 4.2 快速重启（只改了后端代码）

```bash
ssh ubuntu@159.75.99.61
cd ~/BlogProject/springboot
mvn clean package -DskipTests
sudo systemctl restart blog-backend
```

### 4.3 只更新前端

```bash
ssh ubuntu@159.75.99.61
cd ~/BlogProject/vue
npm run build
sudo cp -r dist/* /var/www/blog/
```

---

## 五、日志查看

```bash
# 查看后端实时日志
sudo journalctl -u blog-backend -f

# 查看最近 50 行
sudo journalctl -u blog-backend -n 50

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log

# 查看 Nginx 访问日志
sudo tail -f /var/log/nginx/access.log
```

---

## 六、常用维护命令

```bash
# 查看后端服务状态
sudo systemctl status blog-backend

# 重启后端
sudo systemctl restart blog-backend

# 停止后端
sudo systemctl stop blog-backend

# 启动后端
sudo systemctl start blog-backend

# 查看 Nginx 状态
sudo systemctl status nginx

# 重载 Nginx 配置（不中断服务）
sudo nginx -s reload

# 测试 Nginx 配置是否正确
sudo nginx -t

# 查看端口占用
sudo netstat -tlnp | grep 8080    # 后端端口
sudo netstat -tlnp | grep 80      # 前端端口

# 查看磁盘空间
df -h

# 查看内存使用
free -h

# 查看进程
ps aux | grep java
```

---

## 七、腾讯云 COS 配置

COS 凭证在仓库的 application.yaml 中已移除（安全原因）。

**本地开发**：凭证在 application-local.yaml（已 gitignore）
**服务器**：手动加到 /home/ubuntu/blog/application.properties：

```properties
cos.secret-id=你的COS_SECRET_ID
cos.secret-key=你的COS_SECRET_KEY
cos.bucket-name=blog-photos-1453444415
cos.region=ap-guangzhou
```

添加后重启后端：
```bash
sudo systemctl restart blog-backend
```

---

## 八、Spring Profile 机制（防止本地/服务器配置不一致）

项目有三个配置层级，优先级从高到低：

| 优先级 | 配置文件 | 适用环境 | 是否提交 Git |
|-------|---------|---------|------------|
| 最高 | 服务器 /home/ubuntu/blog/application.properties | 生产环境 | 否 |
| 中 | application-local.yaml | 本地开发 | 否（被 gitignore） |
| 最低 | application.yaml | 默认/仓库 | 是 |

**本地启动**：
```bash
cd springboot
mvn spring-boot:run -Dspring-boot.run.profiles=local
```

**服务器启动**（systemd 自动加载外部配置）：
```bash
sudo systemctl start blog-backend
# systemd 的服务配置里已经加了 --spring.config.additional-location
```

---

## 九、常见问题

### Q: 启动后端报数据库连接失败？
检查 MySQL 是否运行：
```bash
sudo systemctl status mysql
```
检查数据库名是否正确（服务器用 blog，本地用 code2026）。

### Q: 前端页面空白或 404？
确认 npm run build 成功，且 dist 已复制到 /var/www/blog/。

### Q: 图片上传失败？
检查 COS 配置是否正确，以及 /home/ubuntu/blog/application.properties 里有没有写 COS 凭证。

### Q: 登录提示用户名或密码错误？
首次部署需要先创建 admin 用户。后端启动时 InitRunner 会自动创建 admin / 123456。如果数据库里已经有 user 表但没有数据，试试重启后端。

### Q: 部署后访问 API 返回 502？
Nginx 代理不到后端。检查后端是否启动成功：
```bash
sudo systemctl status blog-backend
curl http://localhost:8080/api/auth/login
```

---

## 十、服务器资源速查

```bash
# 查看服务器公网 IP
curl ifconfig.me

# 查看系统版本
lsb_release -a

# 查看 Java 版本
java -version

# 查看 MySQL 版本
mysql --version

# 查看 Nginx 版本
nginx -v

# 查看 Node 版本
node -v
```