# 个人博客服务器部署指南（安全版）

> 生产环境：腾讯云 Ubuntu 22.04；生产数据库 `blog`；本地数据库 `code2026`。
>
> 本文只保留部署顺序。安全配置的唯一操作依据是 [安全部署指南.md](安全部署指南.md)，不要再把数据库密码、JWT 密钥或 COS 密钥写入仓库中的 YAML。

## 一、备案完成前可以完成的工作

1. 安装 JDK 21、MySQL 8、Nginx 和 Maven。
2. 创建运行用户 `blogapp`，后端安装到 `/opt/blog/springboot.jar`。
3. 复制 `deploy/blog-backend.env.example` 为 `/etc/blog-backend.env`，填写生产密钥并设置权限 `600`。
4. 导入 `init.sql`；已有数据库依次执行 `album_visibility_migration.sql` 和 `security_hardening_migration.sql`。
5. 安装 `deploy/systemd/blog-backend.service`，确认后端只监听 `127.0.0.1:8080`。
6. 构建前端并复制到 `/var/www/blog/`。

后端安装示例：

```bash
cd ~/BlogProject/springboot
mvn clean package
sudo install -o blogapp -g blogapp -m 750 \
  target/springboot-0.0.1-SNAPSHOT.jar /opt/blog/springboot.jar
sudo cp ~/BlogProject/deploy/systemd/blog-backend.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable blog-backend
```

首次启动前先按 `安全部署指南.md` 完成数据库迁移和一次性管理员引导配置，成功后删除引导密码变量并关闭引导开关。

前端安装示例：

```bash
cd ~/BlogProject/vue
npm ci
npm run build
sudo mkdir -p /var/www/blog
sudo cp -r dist/. /var/www/blog/
```

## 二、域名和备案完成后

1. 将域名 A 记录解析到服务器公网 IP。
2. 在腾讯云申请免费 DV SSL 证书，下载 Nginx 格式证书。
3. 按 [安全部署指南.md](安全部署指南.md) 安装证书并启用 `deploy/nginx/blog.conf.template`。
4. 安全组只开放 80、443；SSH 22 优先限制为管理员固定 IP；不要开放 8080 和 3306。
5. 执行 `sudo nginx -t` 后重载 Nginx，再完成文档中的安全验收。

在 HTTPS 正式可用前，不要通过公网 HTTP 输入管理员密码。
