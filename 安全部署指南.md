# 博客公网安全部署指南

## 1. 上线前准备

1. 备份 `blog` 数据库和 COS 对象。
2. 依次执行尚未执行的相册迁移与 `security_hardening_migration.sql`；执行命令必须显式指定生产库 `blog`，脚本本身不会切换数据库。安全迁移会先禁用旧管理员，直到完成下面的一次性引导。
3. 腾讯云 COS 中确认私密照片对象 ACL 为“私有读写”；首次部署可临时设置 `ALBUM_SYNC_ACL_ON_STARTUP=true` 启动一次，同步完成后立即改回 `false`。
4. 创建最小权限 COS 子账号，只授予目标 Bucket 的上传、下载、删除及对象 ACL 权限，不使用主账号永久密钥。
5. 如果旧环境还存在当前代码不再使用的 `admin` 表，先备份并统计其密码是否为 BCrypt；确认不再需要旧登录数据后再清理。不要让包含旧密码的数据库备份进入 Git 或公网存储。

## 2. 后端密钥与进程

```bash
sudo useradd --system --home /nonexistent --shell /usr/sbin/nologin blogapp
sudo mkdir -p /opt/blog
sudo chown -R blogapp:blogapp /opt/blog

openssl rand -base64 64
sudo cp deploy/blog-backend.env.example /etc/blog-backend.env
sudo chmod 600 /etc/blog-backend.env
sudo chown root:root /etc/blog-backend.env

sudo cp deploy/systemd/blog-backend.service /etc/systemd/system/blog-backend.service
sudo systemctl daemon-reload
sudo systemctl enable blog-backend
```

将打包后的 JAR 安装为 `/opt/blog/springboot.jar`，真实数据库密码、JWT 密钥和 COS 密钥只写入 `/etc/blog-backend.env`。后端仅绑定 `127.0.0.1:8080`，腾讯云安全组不得开放 8080 和 3306。

本地真实配置固定放在 `springboot/application-local.yaml`（已被 Git 忽略），不得放进 `src/main/resources`；这样生产 JAR 不会夹带本地数据库或 COS 密钥。

数据库运行账号只授予 `blog` 库的 `SELECT, INSERT, UPDATE, DELETE`；结构迁移使用单独账号执行。

### 一次性创建管理员

仓库不保存真实管理员密码，也不保存真实密码的 BCrypt 哈希。首次部署或执行安全迁移后，在 `/etc/blog-backend.env` 中临时加入：

```bash
BOOTSTRAP_ADMINS_ENABLED=true
BOOTSTRAP_YEEEE_PASSWORD='一组全新且至少14字符的强密码'
BOOTSTRAP_CHIP_PASSWORD='另一组不同的全新强密码'
```

不要继续使用曾经在需求对话或公开 Git 历史中出现过的旧密码。启动并看到“管理员一次性引导完成”后，立即删除两个密码变量，将开关改回 `false`，再重启服务：

```bash
sudo systemctl start blog-backend
sudo journalctl -u blog-backend -n 100 --no-pager
sudoedit /etc/blog-backend.env
sudo systemctl restart blog-backend
```

如果未提供两组不同且至少14字符的密码，引导会拒绝执行；正常运行时必须保持引导开关关闭。

## 3. 腾讯云免费证书与 Nginx

备案和 DNS 生效后，在腾讯云 SSL 证书控制台申请免费 DV 证书并完成 DNS 验证。下载 Nginx 格式证书，将证书链和私钥安装到 `/etc/nginx/ssl/`，权限设为私钥 `600`、目录 `700`。

复制 `deploy/nginx/blog.conf.template` 到 `/etc/nginx/sites-available/blog`，替换全部 `YOUR_DOMAIN` 和证书文件名，然后执行：

```bash
sudo nginx -t
sudo systemctl reload nginx
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

腾讯云安全组只开放 80、443；22 端口优先限制为管理员固定 IP。确认 HTTPS 稳定后再长期保留 HSTS。

## 4. 验收

- 未登录只能 GET 公开内容，所有写请求返回 401。
- 普通或禁用账号不能登录；`yeeee` 与 `chip` 可独立同时在线。
- 同一账号重新登录后，旧 Token 立即返回 401。
- 私密照片直链匿名访问失败，管理员获得的签名 URL 约 10 分钟后失效。
- 连续输错密码 5 次后，同一 IP 被限制 15 分钟。
- HTML 事件属性、`javascript:` URL 和伪造图片上传均被拒绝或清洗。
- `https://YOUR_DOMAIN` 正常访问，HTTP 自动跳转 HTTPS，公网 IP 不能绕过域名访问。
